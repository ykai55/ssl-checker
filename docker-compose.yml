version: "3.8"

services:
  ssl-checker:
    image: node:20-slim
    working_dir: /app
    command: sh -c "npm ci && npm run build && node dist/index.js"
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./:/app
      - /app/node_modules
    restart: unless-stopped
